import { TemplateHelpdataElement } from './template-helpdata-element';

describe('TemplateHelpdataElement', () => {
  it('should create an instance', () => {
    expect(new TemplateHelpdataElement()).toBeTruthy();
  });
});
